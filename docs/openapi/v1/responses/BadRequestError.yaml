description: Something is wrong with the data provided
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: integer
              example: 400
            message:
              type: string
              # TODO: revise this, might return json with errors
              example: 'Bad request'